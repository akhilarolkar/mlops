# nginx.conf
events {}

http {
    server {
        listen 80;

        location / {
            # "api" will be the name of our Flask service in Docker Compose
            proxy_pass http://api:5000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}